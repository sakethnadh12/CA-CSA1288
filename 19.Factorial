#include<stdio.h>
int main()
{
	int fact=1,n,i;
	printf("enter number:");
	scanf("%d",&n);
	for(i=1;i<=n;i++)
	{
	fact*=i	;
	}                                                                                
	printf("factorial of number is:%d",fact);
	return 0;
}
